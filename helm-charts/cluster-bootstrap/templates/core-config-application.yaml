apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: core-config-bootstrap
  namespace: {{ .Values.metadata.namespace }}
spec:
  project: {{ .Values.spec.project }}

  source:
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: HEAD
    path: helm-charts/cluster-core-config
  destination: 
    server: {{ .Values.spec.destination.server }}
    namespace: name

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true